{
  "name": "course-discovery-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Playwright E2E suite for the Course Discovery plugin's a11y-critical paths (no-JS filtering, keyboard combobox).",
  "scripts": {
    "test": "playwright test",
    "test:list": "playwright test --list",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
